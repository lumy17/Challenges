@page
@model Challenges.Pages.RealizariUtilizatori.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<div class="other_pages">
    <h1>Index</h1>

    <p>
        <a asp-page="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.RealizareUtilizator[0].Utilizator)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.RealizareUtilizator[0].Realizare)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.RealizareUtilizator[0].Data)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.RealizareUtilizator)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Utilizator.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Realizare.Id)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Data)
                    </td>
                    <td>
                        <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                        <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                        <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

