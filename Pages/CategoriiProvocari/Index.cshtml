@page
@model Challenges.Pages.CategoriiProvocari.IndexModel

@{
    ViewData["Title"] = "Index";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CategorieProvocare[0].Categorie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CategorieProvocare[0].Provocare)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.CategorieProvocare) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Categorie.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Provocare.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
