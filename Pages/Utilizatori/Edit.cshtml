@page
@model Challenges.Pages.Utilizatori.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Pages/Shared/_Layout.cshtml";
}

<h1>Edit</h1>

<h4>Utilizator</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Utilizator.Id" />
            <div class="form-group">
                <label asp-for="Utilizator.Email" class="control-label"></label>
                <input asp-for="Utilizator.Email" class="form-control" />
                <span asp-validation-for="Utilizator.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Utilizator.Nume" class="control-label"></label>
                <input asp-for="Utilizator.Nume" class="form-control" />
                <span asp-validation-for="Utilizator.Nume" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Utilizator.Prenume" class="control-label"></label>
                <input asp-for="Utilizator.Prenume" class="form-control" />
                <span asp-validation-for="Utilizator.Prenume" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Utilizator.NumarTelefon" class="control-label"></label>
                <input asp-for="Utilizator.NumarTelefon" class="form-control" />
                <span asp-validation-for="Utilizator.NumarTelefon" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label>Categorii de Interes</label>
                @foreach (var category in Model.ListaCategorii)
                {
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="category-@category.Id" name="SelectedCategories" value="@category.Id" @(Model.SelectedCategories.Contains(category.Id) ? "checked" : "")>
                        <label class="form-check-label" for="category-@category.Id">@category.Nume</label>
                    </div>
                }
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
